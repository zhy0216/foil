difficulty: hard
agent: inherit

# Verify the installed extension and document delivery

Read [the complete plan](../../plan.md), [routing](../README.md), and all implementation handoffs. Start after `03-share-link-import.md` is merged (01/02 transitively). One isolated worktree, one final commit.

## T1 · Add real extension fixtures and local persistence coverage

- Work: create `apps/extension/playwright.config.ts` and fixtures/tests under `apps/extension/tests/e2e/`. Load the production `dist` with Playwright's bundled Chromium, `channel: 'chromium'`, `launchPersistentContext` and isolated temporary profiles. Resolve the actual extension ID from its worker. Keep service workers enabled; do not inherit the website fixture's blocking policy or substitute an HTTP preview for extension execution.
- Work: exercise the packaged page under normal manifest CSP. Check worker/asset/resource paths, page errors and CSP violations. Combine a meaningful launch-listener test from 02 with a real toolbar smoke where possible; be explicit if the browser runner cannot drive native toolbar UI.
- Work: verify editing/formatting, library create/switch/rename/delete, comments and settings, reload/close/reopen, a browser-profile restart and two tabs editing different documents. Cover offline first use of the editor and lazy resource/crypto paths, and retain visible unsaved/error behavior rather than silently losing failed writes. Ensure tests clean up profiles and contexts.
- Expected files: extension Playwright config, `tests/e2e/fixtures.ts`, persistence/smoke specs and helpers; extension package scripts/dev dependencies and `bun.lock` if needed.
- Acceptance: tests operate on the real `chrome-extension://` origin with the manifest loaded and no security bypasses; the actual compiled UI preserves documents/settings as specified and remains separate from HTTP website storage. All unexpected HTTP(S) requests fail the test. Failure artifacts are retained in ignored app-local output.
- Dependencies: `03-share-link-import.md` (01/02 transitively).

## T2 · Verify cross-host share links, imported links and actual HTML files

- Work: cover ordinary/password/time/combined links generated by the extension and opened in independent website recipient contexts. Assert public/configured HTTP(S) origin/path and absence of extension URLs; satisfy recipients from a separately served local website build or deterministic route fulfillment instead of contacting the public host. Verify title/Markdown/comments, protected plaintext hiding, wrong-password retry, cancellation, time gates and explicit forks.
- Work: exercise **Open shared link** with valid four-mode links plus malformed/oversized/unsafe input and cancelled/API-error cases. Assert no request/navigation to pasted hosts, no mutation of the source document, fragment stripping, read-only imported state and explicit library fork.
- Work: click actual Copy and Export HTML controls. Verify clipboard success from a user gesture and the existing fallback on denial without adding a clipboard-read permission. Save real downloads, inspect the shared data and CSP, and open/re-export actual `file://` files in fresh Chromium and WebKit contexts. Ordinary/password files must work offline, including first creation and re-export; both time modes use fixed drand information/beacon fixtures. Reuse or deliberately share existing `apps/web/tests/e2e/helpers/` fixtures without introducing runtime app dependencies or broad unrelated test refactors.
- Expected files: extension sharing/import/export specs and fixtures; narrowly reused/extracted website test helpers or test support if appropriate; existing website tests only for needed integration corrections.
- Acceptance: real extension-origin JS, crypto chunks and resource imports satisfy CSP; four protection modes preserve existing wire/file compatibility and never reveal protected content before gates. Exported files contain the public share base and work independently of the installed extension. Intercept all external requests; fulfill only expected local website and drand fixtures, never real public drand. Preserve schema/transport limits and all existing website/file assertions.
- Dependencies: `03-share-link-import.md`; T1 fixture.

## T3 · Integrate package checks and CI without disrupting the website

- Work: make root typecheck/test/build/e2e include the extension automatically. Declare `@foil/extension#test:e2e`'s dependency on the website build if cross-host fixtures require it; do not add a runtime dependency from extension to web. Use a separate strict preview port for extension recipients and declare needed environment variables in Turbo. Unit/KDF validation finishes before builds/browser tests.
- Work: add production package checks for manifest entry paths, icons, packaged lazy modules, local executable code/CSP, narrow permissions and ZIP structure. Prefer assertions tied to loadability/privacy behaviors, not duplicate implementation snapshots. Expand `.github/actions/verify/action.yml` to collect both apps' failure diagnostics and build a reviewable extension ZIP artifact. Adjust workflow limits only if measured runtime requires it. Pages still uploads only `apps/web/dist`; extension package outputs are not website assets.
- Expected files: `turbo.json`, extension package/check/test files, root `package.json` only if required, `.github/actions/verify/action.yml`, `.github/workflows/ci.yml`/`deploy.yml` only for justified configuration, root lockfile when dependencies change.
- Acceptance: root/browser commands build required outputs once with no port/build-directory collisions, fail on missing package assets or violated permissions, and produce the correct extracted ZIP layout. Existing Pages verification/upload paths and default website base remain correct. A frozen install succeeds after all dependency changes.
- Dependencies: `03-share-link-import.md`; T1/T2.

## T4 · Document installation, privacy and actual final results

- Work: update `apps/extension/README.md`, root `README.md`, `CLAUDE.md`, and any current brand/source links. Explain Chrome/Edge Developer Mode → Load unpacked → `apps/extension/dist`, pin/click behavior, build/watch/reload/package commands, artifact paths, share-host configuration, explicit URL import and independent website/extension libraries. Distinguish ordinary/password offline behavior from drand-dependent time capsules. Explain local storage clearing/uninstall and export backups without claiming cloud sync or automatic migration.
- Work: report support based on actual checks. Automated Chromium tests do not establish a manual Chrome/Edge installation; record manual smoke/browser versions if available. A browser-extension ZIP is the local deliverable, not a published store listing.
- Validation: select pinned Node 22.22.3/Bun 1.4.2; run `bun install --frozen-lockfile`, root `bun run typecheck`, `bun run test`, `bun run build`, `bun run test:e2e:install --with-deps` if needed, `bun run test:e2e`, `bun audit`, `bun audit --prod`, and `bun run --cwd apps/extension package`. Run sequentially, then the separate root-path website build and package-level e2e commands in the plan. Do not rebuild `/` and `/foil/` into the same output concurrently. Restore the default website artifact if a subsequent upload/package check expects it.
- Expected files: README/CLAUDE/brand references and completion records; minimal fixes uncovered by actual regressions within this task's scope, with coordinator notification for ownership changes.
- Acceptance: all required suites and package checks pass, baseline tests remain covered, ZIP/dist locations and exact commands/toolchain/browser results are recorded, and any material limitation is stated. Do not skip tests, use live external fixtures, weaken CSP or suppress functional failures to complete the queue. No store submission, deployment or push.
- Dependencies: `03-share-link-import.md`; T1–T3.


## Completion and handoff · 2026-09-08

Completed only task 04 on `herdr/plan-browser-extension-04-extension-regressions`, starting at integrated task 03 commit `7b195b6c99688c744b5cce21b40e412b3ec92cb1` (01/02 transitively). The single local task commit is `test(extension): verify installed editor and document delivery` (the commit containing this archive; its hash is reported in the final response). Integration awaits explicit coordinator instruction. Routing remains `agent: inherit`, Codex / `gpt-6-astra` / `max`. The queue now records 03 as integrated; the plan's final execution section remains unchanged for the coordinator.

### Files and implementation contract

- Added `apps/extension/playwright.config.ts` and `tests/e2e/{fixtures,helpers,smoke.spec,persistence.spec,sharing.spec,import.spec,delivery.spec,package.spec}.ts`. There are **27 permanent cases**: 19 in `installed-chromium`, four in `file-chromium` and four in `file-webkit`. Every extension author/import runs in the real production MV3 package, with `channel: 'chromium'`, `launchPersistentContext`, a worker-derived extension ID, isolated profiles and service workers enabled. Chromium/WebKit file recipients navigate to actual downloaded `file://` files.
- `Sessions` registers each launched persistent context before monitoring/worker startup can fail. Startup failure closes that context even without a `Session`. Teardown attempts all session closures, all tracked context closures and every owned profile cleanup, aggregates errors, and deletes only its own `mkdtemp` directories. Strict per-context HTTP(S), page, console and CSP assertions include lifecycle requests and closed/reloaded pages. Playwright retains failure traces/screenshots; extra per-context JSON and screenshots live in ignored app-local output. Two targeted cleanup failure probes cover real failed startup and continued cleanup after synchronous/asynchronous teardown errors.
- Reused only existing website test helpers through relative **test-only imports**. The hostile document, exact comments/Markdown, fixed quicknet info and verified round **992**, download inspection, file CSP and read-only assertions are shared. No runtime web dependency, protocol/schema/transport/storage change, security bypass or live drand was introduced. All preexisting website/file tests and helpers are byte-preserved; only the website config's local worker default becomes two.
- Added extension `test:e2e` and `package:dist` scripts and TypeScript discovery for the tests/config. No dependency or lockfile change was needed. Turbo explicitly connects extension e2e to both app builds and checked ZIP packaging; build/typecheck include external helper inputs alongside `$TURBO_DEFAULT$`. Build output and ZIP caches are declared; browser tests remain uncached. Source-only shared editor edits invalidate both builds. The website and extension recipient previews use independent strict ports, with explicit `FOIL_E2E_BASE`, port, `VITE_*` and Playwright environment handling.
- Root `test:e2e` uses `--continue=dependencies-successful`: independent suites finish and clean up after a sibling failure while the root still fails, and downstream tasks cannot run after failed dependencies. The verification action explicitly builds after units, runs both browser suites, uploads the checked extension ZIP and retains both apps' failure diagnostics. Existing Pages workflows still upload **only `apps/web/dist`**. The existing 15-minute workflow limit is unchanged; no measured local need justified increasing it.
- Strengthened `apps/extension/scripts/artifact.ts` to parse and validate the bundled standalone resource as inert JSON without executing it. Two new unit cases reject empty/indirect resource data; production package cases reject missing worker, icon, reader or lazy crypto files and broadened permissions. The actual checked ZIP is extracted, byte-compared to dist, installed, and used for an offline HTML download.
- Updated root/extension README, `CLAUDE.md` and current brand/source guidance for Developer mode/Load unpacked, pin/click, build/watch/reload/package, artifact paths, public share-host configuration, explicit import/fork, independent local libraries, data clearing/uninstall, export backups and offline/time-capsule differences. No cloud sync, automatic migration, store listing or manual-install coverage is claimed.

### Coordinator notice: minimal source correction

The real installed password-cancellation regression found a shared App race: after Cancel restored the local editor, an already-running AES/decompression result could still replace its view with the imported document. The seven-line change in `packages/editor/src/App.tsx` invalidates password results on cancellation, unmount or a newer unlock attempt before applying state. This is the only production editor behavior change and falls within the authorized regression corrections. Four new `App.password-cancel.test.tsx` cases cover cancelled late plain/time/error results and superseded attempts; the installed test holds an **actual successful AES result**, cancels, then releases and observes actual parsing before asserting the local state remains intact. Successful import, crypto and Chrome APIs are never mocked. This shared-source ownership correction is explicitly handed to the coordinator for review.

### Per-acceptance evidence

| Acceptance | Observed evidence |
| --- | --- |
| T1: installed MV3/CSP/local assets and toolbar | `smoke.spec.ts` loads the actual manifest/worker/editor, checks narrow permissions/CSP/local paths and records compiled lazy scripts through CDP. Existing background listener units remain; DevTools invokes the actual compiled toolbar handler twice and real Chrome APIs create two packaged editor tabs. Installation opens none. This is handler evidence, not a native toolbar click. |
| T1: local persistence and document lifecycle | `persistence.spec.ts` edits and formats text, creates/switches/renames/deletes documents (including cancelled deletion), creates comments and changes theme/font size. Reload, close/reopen with a pending debounced write, full browser-profile restart and two differently bound tabs preserve expected documents/settings. Same-profile HTTP website documents and preferences remain independent of extension storage. |
| T1: offline first use and failed writes | Installed profiles start offline; ordinary/password first creation and first HTML export load local resources/crypto. Offline drand sealing shows its actual visible error. Quota failure preserves the previous stored document, keeps edits visibly unsaved and blocks switching until the real write path recovers. Profiles and contexts are cleaned after successes and failures. |
| T2: four outgoing modes | `sharing.spec.ts` creates `d/e/td/te` links in the extension and visits their actual public URL in independent website contexts, fulfilled only from a separate local build. Exact title/Markdown/comments, HTTP(S) public origin/path, absence of extension URLs, protected plaintext hiding, wrong-password retry, time gates, no premature drand, explicit fork/reload and unchanged author library all pass. Four more targeted cases pass with configured `https://zhy0216.github.io/foil/?from=extension#discarded`, normalized to the `/foil/` public base. |
| T2: four imported modes | `import.spec.ts` creates all four links in the real website, pastes a never-contacted custom host or raw fragment, and uses actual `chrome.tabs.create`. Final extension URLs strip fragments; imported content stays read-only and unpersisted/unbound until explicit **Edit anyway**. Exact document/comment fork contents survive reload, with source and website libraries unchanged. Wrong passwords, password/time cancellation and early time gates remain hidden. |
| T2: invalid input and failure isolation | Installed native modal tests reject unsafe/credential-bearing, malformed, oversized and unsupported input; cancel/Escape restores focus and the background stays inert. Synchronous/asynchronous API failures alone are injected, retain input and safe diagnostics, and preserve all source documents/tabs; retry restores and uses the real API. Valid transport containing invalid document schema remains unpersisted. No pasted-host request/navigation occurs. |
| T2: cancellation, drand integrity and retries | Real AES late-result cancellation passes after the App fix. Fixed drand info failover visits exactly the four permitted origins; a forged beacon fails closed with its specific expected diagnostic. Offline decrypt failure retries successfully. A held actual beacon is released after Cancel; the result is decoded but cannot replace the restored local document, and an explicit new import succeeds. Never a live drand request. |
| T2: Copy and actual downloads | `smoke.spec.ts` clicks actual **Copy**, then pastes the OS clipboard through a user keyboard gesture; clipboard denial exercises the existing manual-copy fallback without clipboard-read permission. `delivery.spec.ts` clicks actual **Export HTML**, saves real downloads, inspects resource/share metadata and CSP, and proves protected files/filenames hide content. |
| T2: Chromium/WebKit file compatibility | Eight cases open actual files in fresh Chromium/WebKit contexts, refresh, re-export through the reader's actual control and reopen in another fresh context. Ordinary/password flows reject all HTTP(S), including first export and re-export. Both time modes use fixed drand gates/failure/retry. Re-export resets protection; tests explicitly reselect it and change the password, reject the old one and verify the exact document. All non-payload HTML bytes and runtime/styles remain identical; cross-engine compressed/encrypted payload lengths need not match. Continued links open the independent public website. All 28 existing website/file cases pass at each website base. |
| T3: loadability and package policy | `package.spec.ts` checks all ZIP entries against the actual dist, extracts with manifest at root and loads that extraction in a fresh offline persistent Chromium profile. Missing assets/resources and broader capabilities fail package checks. Final package check validates 12 runtime files, all local executable code/imports, CSP, icons, reader resource and permissions. |
| T3: root discovery/cache/CI/environment | Final root browser graph runs two builds once (both cached), packages once and runs both apps with ports 4483/4484, no collisions. Dry probes verify editor-source/helper invalidation, default package inputs, VITE build hashes, independent preview ports, both recipient-base variables and Playwright forwarding. CI artifact paths cover both apps; Pages path stays `apps/web/dist`, outside extension dist/ZIP. Frozen install and both audits pass. |
| T4: installation/privacy/results | Updated docs explain local Chrome/Edge installation, toolbar tab behavior, commands and backups. Actual browser versions are Chromium **153.0.8010.12** and WebKit **26.6**. There is no manual branded Chrome/Edge installation or native toolbar click evidence, and no publication/deployment. The default `/foil/` website and default public extension artifacts have been restored and verified. |

### Actual commands, counts and wall times

All commands ran in this task worktree with `PATH=/home/ubuntu/.nvm/versions/node/v22.22.3/bin:$PATH` (**Node v22.22.3**, **Bun 1.4.2**) and `TURBO_CACHE_DIR="$PWD/.turbo/cache"`. Commands were timed with `/usr/bin/time -p`; values below are measured wall seconds. Frozen install, typecheck, full units and build completed sequentially before browsers, including a fresh full sequence after the production cancellation fix. No unit/KDF run overlapped a build/browser run. Browser dependencies were already installed before the final sequence; the required install command also passed earlier.

| Exact command (with the environment above) | Actual result / wall time |
| --- | --- |
| `bun install --frozen-lockfile` | Pass; 176 installs across 227 packages, no changes, 6 ms reported / **0.00 s** rounded wall time. |
| `bun run typecheck` | **3/3 tasks**, uncached, **7.74 s** after the source fix. After cleanup changes: 3/3, **3.66 s**. After final cache inputs: 3/3, **5.03 s** (editor/web cached). |
| `bun run test` | **876/876**: shared **635** in 19 files, extension **241** in five files; both tasks uncached, **25.97 s**. Baseline 870 remains covered, including all 619 originals. No skipped tests. |
| `bun run build` | Both apps uncached, **7.39 s** after the source fix; after fixture cleanup **7.16 s**. Final default restore after alternate/override builds: **8.35 s**, extension built and website restored from cache. |
| `bun run test:e2e:install --with-deps` | Passed, bundled Chromium/WebKit and Linux dependencies available, **8.34 s**. |
| `FOIL_E2E_PORT=4483 FOIL_EXTENSION_E2E_PORT=4484 bun run test:e2e` | **55/55**: website **28** (14 Chromium + 14 WebKit); extension **27** (19 installed + four Chromium files + four WebKit files). **97.74 s** wall, Turbo 97.657 s; five tasks successful, two build cache hits, zero skips/flaky/unexpected results. Extension Playwright duration 96.596 s. |
| `npm_config_registry=https://registry.npmjs.org bun audit` | No vulnerabilities, **221 packages**, **0.06 s**. |
| `npm_config_registry=https://registry.npmjs.org bun audit --prod` | No vulnerabilities, **37 packages**, **0.11 s**. |
| `bun run --cwd apps/extension package` | Build/check/ZIP passed, **7.37 s**, 12 runtime files, 315,783-byte ZIP. |
| `bunx --no-install turbo run build --filter=@foil/web -- --base /` | Separate root website build passed, **6.83 s**. |
| `FOIL_E2E_BASE=/ FOIL_E2E_PORT=4485 bun run --cwd apps/web test:e2e --workers=2` | **28/28**, 14 Chromium + 14 WebKit, **73.42 s**, zero skips/failures. |
| `VITE_FOIL_SHARE_BASE_URL='https://zhy0216.github.io/foil/?from=extension#discarded' bunx --no-install turbo run build --filter=@foil/extension` | Configured extension build passed, **7.20 s**, after root website validation. |
| `VITE_FOIL_SHARE_BASE_URL='https://zhy0216.github.io/foil/?from=extension#discarded' FOIL_E2E_BASE=/ FOIL_EXTENSION_E2E_PORT=4486 bun run --cwd apps/extension test:e2e tests/e2e/sharing.spec.ts tests/e2e/delivery.spec.ts --project=installed-chromium --project=file-chromium --grep '(^| )e: installed Export HTML\|packaged author shares' --workers=2` | **5/5**, four public link modes plus password file refresh/re-export/reopen, **18.01 s** wall / 17.327 s Playwright. The actual shell regex contains a literal `|` alternation, not a backslash; it is escaped here for Markdown table rendering. |
| `bun run --cwd apps/extension check` after default restore | All **12** runtime files passed, **0.06 s**; ZIP root/integrity and every extracted byte also matched the restored dist. |
| `bunx --no-install turbo run build typecheck test:e2e --dry=json` and controlled temporary input/environment probes | Passed helper/editor/default-input invalidation, env forwarding/port isolation, graph dependencies/outputs and exact source/hash restoration. No builds/browsers run by probes. |
| `bun /tmp/foil-04-validation/cleanup-probe.ts` | Both focused failure-cleanup probes passed, **0.82 s**: real context closes after startup failure; every closure/profile cleanup is attempted despite synchronous/asynchronous errors, and an unregistered control directory remains. |

The configured-share command is also recorded without Markdown escaping in `logs/e2e-share-override.log`. After it, `bun run build` restored **both default artifacts**; no variants wrote one output directory concurrently. Only cache/test-fixture/documentation changes followed the successful full functional validation. Those received targeted cleanup/cache checks, typecheck and a default build/artifact comparison, without another redundant full browser or KDF run.

### Diagnosed failures and warnings

| Preliminary failure | Diagnosis and resolution |
| --- | --- |
| Initial typecheck, 7.72 s | Three new-test typing errors: two `DocState` matcher arguments needed object spreading and Chrome's MV2/MV3 CSP union needed narrowing. Fixed the new tests; typecheck passed before browsers. |
| First root browser attempt, 30.82 s: extension 26 failed / one static case passed; sibling website interrupted | The new fixture manually started tracing that Playwright had already started on custom contexts. Removed duplicate tracing and used Playwright's configured retained traces. The next installed smoke/persistence selection passed **6/6**, 13.07 s. No product/CSP failure was suppressed. |
| Sharing/import/file/package selection, 68.10 s: 15 passed / six failed | One exposed the real cancelled-password App race described above. Five were new file-size equality assumptions: Chromium time-envelope randomness and WebKit gzip differences can change payload size. Compared exact non-payload HTML, runtime/styles and metadata instead, then required exact decoded full-document/protection round trips. Existing website/file assertions were never weakened. |
| Root retry, 0.89 s, strict preview port 4481 occupied | Default Turbo cancellation from the first failed run left this task's website preview alive. Verified its command/worktree ownership, terminated only that process, kept strict no-reuse policy, and added dependency-safe continuation so independent suites finish teardown. Subsequent ports were unique. |
| Next root attempt, 98.27 s: website 28 passed, extension 26 passed / one failed | The fixed cancellation behavior passed, but its context-wide probe ran in Chromium's initial opaque `about:blank`, where WebCrypto was unavailable. Strict page-error assertions caught it. Moved observation to the actual gated secure extension page; focused real-AES case passed **1/1**, 4.79 s, then final root passed 55/55. |
| Cleanup and cache review probes | Coordinator identified ownership registration before monitor and continuing teardown after an error; both are fixed and independently probed. The first broken tracing fixture had left 26 profiles: each was proven by its preferences to load this worktree's dist/extracted ZIP and had no live owner before removal. Only those task-created directories were removed. External helper imports initially did not invalidate extension compilation; explicit default-plus-helper inputs fix it. A dry-probe assumption that VITE would leave typecheck hashes unchanged was corrected after observing Turbo's automatic Vite framework env inference. |

Final context diagnostics contain no unexpected HTTP(S), page errors or CSP violations. Console checking permits only the exact explicitly aborted drand resource failures and the single expected forged-signature diagnostic; all other errors fail. Final full run retained 59 monitored context diagnostic files, with zero leaked temporary extension-test profiles and no task-owned live browser/preview processes after cleanup. Known `ReactDOMTestUtils.act` deprecation/ShareModal act warnings, `NO_COLOR`/`FORCE_COLOR` warnings and preview SIGTERM exit-143 messages are retained; the successful suite processes exit zero. No skip, retry, hidden functional error or relaxed CSP was used.

### Artifacts, review and limits

Worktree root: `/home/ubuntu/.herdr/worktrees/foil/herdr-plan-browser-extension-04-extension-regressions`.

- **Unpacked extension:** `apps/extension/dist/`, **12 runtime files**, **916,640 uncompressed bytes**. Final worker is `background.js`; main editor `assets/index-BXzGFgrB.js`; local time crypto `assets/timecapsule-crypto-P2LaV9B3.js`; standalone resource `foil-standalone.js`; manifest and all icons are at package-relative paths.
- **ZIP:** `apps/extension/artifacts/foil-extension-0.1.0.zip`, **315,783 bytes**, SHA-256 **`b48f479a75b84716fe5b9652c1626cf556397153c402029b7631bc65ba5731d0`**. Manifest is at root; no sources, dependencies, tests, profiles, reports or nested dist. Final restored dist is byte-identical to this tested ZIP.
- **Default website:** `apps/web/dist/`, restored `/foil/` asset paths, main `assets/index-CxL8c-_-.js`. Both hosts' reader resource is byte-identical to prior tasks, SHA-256 **`7dbac5572e21a800c27be97e947740d87a280a75205efe11ac6fc5882c458173`**. Pages upload path and standalone website/file behavior remain intact.
- **Saved local review evidence:** `apps/extension/artifacts/validation/README.md` indexes `default/{extension,web}/{playwright-report,test-results}/`, `root-path/web/`, `share-override/extension/` and `logs/`. The default extension `test-results/results.json` and per-context JSON include counts, versions, CSP/network findings; real downloads and the ZIP extraction are retained. Logs/probes also remain in `/tmp/foil-04-validation/`. These outputs are ignored and outside production artifacts. Top-level app Playwright reports show the most recent package-level runs; use the saved default reports for the full 55-case run.
- **Baseline:** all 18 preexisting shared test files and every website/file spec/helper match task 03 byte for byte. There are 876 passing units (870 prior + four App race cases + two resource-package cases). No dependency or lockfile change, website source change, manifest/permission change or other-plan work.
- **Limits:** automated extension coverage is bundled Chromium 153.0.8010.12; WebKit 26.6 coverage is actual HTML files and website recipients, not an extension. No native toolbar click or manual branded Chrome/Edge installation was performed. Documentation describes how to install; it is not manual evidence. CI configuration/artifacts were checked locally, not by running remote CI. No public drand, public-host request, store submission, deployment, push or PR. No remaining functional blocker; coordinator independently validates before integration.

All task 04 acceptance is complete. No extra task agents, branch switch, rebase/merge, stash, worktree deletion or other queue were used. The task has one local implementation commit; await explicit integration and leave the coordinator's plan execution section untouched.
