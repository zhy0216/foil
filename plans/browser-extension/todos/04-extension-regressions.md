difficulty: hard
agent: inherit

# Verify the installed extension and document delivery

Read [the complete plan](../plan.md), [routing](README.md), and all implementation handoffs. Start after `03-share-link-import.md` is merged (01/02 transitively). One isolated worktree, one final commit.

## T1 · Add real extension fixtures and local persistence coverage

- Work: create `apps/extension/playwright.config.ts` and fixtures/tests under `apps/extension/tests/e2e/`. Load the production `dist` with Playwright's bundled Chromium, `channel: 'chromium'`, `launchPersistentContext` and isolated temporary profiles. Resolve the actual extension ID from its worker. Keep service workers enabled; do not inherit the website fixture's blocking policy or substitute an HTTP preview for extension execution.
- Work: exercise the packaged page under normal manifest CSP. Check worker/asset/resource paths, page errors and CSP violations. Combine a meaningful launch-listener test from 02 with a real toolbar smoke where possible; be explicit if the browser runner cannot drive native toolbar UI.
- Work: verify editing/formatting, library create/switch/rename/delete, comments and settings, reload/close/reopen, a browser-profile restart and two tabs editing different documents. Cover offline first use of the editor and lazy resource/crypto paths, and retain visible unsaved/error behavior rather than silently losing failed writes. Ensure tests clean up profiles and contexts.
- Expected files: extension Playwright config, `tests/e2e/fixtures.ts`, persistence/smoke specs and helpers; extension package scripts/dev dependencies and `bun.lock` if needed.
- Acceptance: tests operate on the real `chrome-extension://` origin with the manifest loaded and no security bypasses; the actual compiled UI preserves documents/settings as specified and remains separate from HTTP website storage. All unexpected HTTP(S) requests fail the test. Failure artifacts are retained in ignored app-local output.
- Dependencies: `03-share-link-import.md` (01/02 transitively).

## T2 · Verify cross-host share links, imported links and actual HTML files

- Work: cover ordinary/password/time/combined links generated by the extension and opened in independent website recipient contexts. Assert public/configured HTTP(S) origin/path and absence of extension URLs; satisfy recipients from a separately served local website build or deterministic route fulfillment instead of contacting the public host. Verify title/Markdown/comments, protected plaintext hiding, wrong-password retry, cancellation, time gates and explicit forks.
- Work: exercise **Open shared link** with valid four-mode links plus malformed/oversized/unsafe input and cancelled/API-error cases. Assert no request/navigation to pasted hosts, no mutation of the source document, fragment stripping, read-only imported state and explicit library fork.
- Work: click actual Copy and Export HTML controls. Verify clipboard success from a user gesture and the existing fallback on denial without adding a clipboard-read permission. Save real downloads, inspect the shared data and CSP, and open/re-export actual `file://` files in fresh Chromium and WebKit contexts. Ordinary/password files must work offline, including first creation and re-export; both time modes use fixed drand information/beacon fixtures. Reuse or deliberately share existing `apps/web/tests/e2e/helpers/` fixtures without introducing runtime app dependencies or broad unrelated test refactors.
- Expected files: extension sharing/import/export specs and fixtures; narrowly reused/extracted website test helpers or test support if appropriate; existing website tests only for needed integration corrections.
- Acceptance: real extension-origin JS, crypto chunks and resource imports satisfy CSP; four protection modes preserve existing wire/file compatibility and never reveal protected content before gates. Exported files contain the public share base and work independently of the installed extension. Intercept all external requests; fulfill only expected local website and drand fixtures, never real public drand. Preserve schema/transport limits and all existing website/file assertions.
- Dependencies: `03-share-link-import.md`; T1 fixture.

## T3 · Integrate package checks and CI without disrupting the website

- Work: make root typecheck/test/build/e2e include the extension automatically. Declare `@foil/extension#test:e2e`'s dependency on the website build if cross-host fixtures require it; do not add a runtime dependency from extension to web. Use a separate strict preview port for extension recipients and declare needed environment variables in Turbo. Unit/KDF validation finishes before builds/browser tests.
- Work: add production package checks for manifest entry paths, icons, packaged lazy modules, local executable code/CSP, narrow permissions and ZIP structure. Prefer assertions tied to loadability/privacy behaviors, not duplicate implementation snapshots. Expand `.github/actions/verify/action.yml` to collect both apps' failure diagnostics and build a reviewable extension ZIP artifact. Adjust workflow limits only if measured runtime requires it. Pages still uploads only `apps/web/dist`; extension package outputs are not website assets.
- Expected files: `turbo.json`, extension package/check/test files, root `package.json` only if required, `.github/actions/verify/action.yml`, `.github/workflows/ci.yml`/`deploy.yml` only for justified configuration, root lockfile when dependencies change.
- Acceptance: root/browser commands build required outputs once with no port/build-directory collisions, fail on missing package assets or violated permissions, and produce the correct extracted ZIP layout. Existing Pages verification/upload paths and default website base remain correct. A frozen install succeeds after all dependency changes.
- Dependencies: `03-share-link-import.md`; T1/T2.

## T4 · Document installation, privacy and actual final results

- Work: update `apps/extension/README.md`, root `README.md`, `CLAUDE.md`, and any current brand/source links. Explain Chrome/Edge Developer Mode → Load unpacked → `apps/extension/dist`, pin/click behavior, build/watch/reload/package commands, artifact paths, share-host configuration, explicit URL import and independent website/extension libraries. Distinguish ordinary/password offline behavior from drand-dependent time capsules. Explain local storage clearing/uninstall and export backups without claiming cloud sync or automatic migration.
- Work: report support based on actual checks. Automated Chromium tests do not establish a manual Chrome/Edge installation; record manual smoke/browser versions if available. A browser-extension ZIP is the local deliverable, not a published store listing.
- Validation: select pinned Node 22.22.3/Bun 1.4.2; run `bun install --frozen-lockfile`, root `bun run typecheck`, `bun run test`, `bun run build`, `bun run test:e2e:install --with-deps` if needed, `bun run test:e2e`, `bun audit`, `bun audit --prod`, and `bun run --cwd apps/extension package`. Run sequentially, then the separate root-path website build and package-level e2e commands in the plan. Do not rebuild `/` and `/foil/` into the same output concurrently. Restore the default website artifact if a subsequent upload/package check expects it.
- Expected files: README/CLAUDE/brand references and completion records; minimal fixes uncovered by actual regressions within this task's scope, with coordinator notification for ownership changes.
- Acceptance: all required suites and package checks pass, baseline tests remain covered, ZIP/dist locations and exact commands/toolchain/browser results are recorded, and any material limitation is stated. Do not skip tests, use live external fixtures, weaken CSP or suppress functional failures to complete the queue. No store submission, deployment or push.
- Dependencies: `03-share-link-import.md`; T1–T3.
